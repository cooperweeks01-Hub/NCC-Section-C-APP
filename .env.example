# Optional LLM explanation layer — OFF by default.
# The app is fully functional without any key set. The LLM never touches the
# compliance path; it only narrates an already-decided result in plain English.
#
# Copy to `.env` and set a key to enable the optional explain() layer.
VITE_ANTHROPIC_API_KEY=
